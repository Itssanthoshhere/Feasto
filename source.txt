# Feasto Auth Service — Initial Setup

## Initialize Node.js Project

```bash
npm init -y
```

---

## Install TypeScript Globally

```bash
npm i -g typescript
```

---

## Generate TypeScript Configuration

```bash
npx tsc --init
```

---

## Install Production Dependencies

### Express & Core Packages

```bash
npm i express dotenv googleapis jsonwebtoken mongoose axios cors
```

**Packages Installed**

* Express — Web framework
* Dotenv — Environment variable management
* Google APIs — Google OAuth integration
* JSON Web Token — Authentication & authorization
* Mongoose — MongoDB ODM
* Axios — HTTP client
* CORS — Cross-Origin Resource Sharing

---

## Install Development Dependencies

```bash
npm i -D @types/express @types/dotenv @types/jsonwebtoken @types/mongoose @types/axios @types/cors concurrently
```

### TypeScript

```bash
npm i -D typescript
```

---

## Configure Scripts

Add the following scripts inside `package.json`:

```json
"scripts": {
  "dev": "tsc --watch",
  "build": "tsc",
  "start": "node dist/index.js"
}
```

### Recommended (Better DX)

Install:

```bash
npm i -D ts-node-dev
```

Update scripts:

```json
"scripts": {
  "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
  "build": "tsc",
  "start": "node dist/index.js"
}
```

---

## Create Source Directory

```bash
mkdir src
touch src/index.ts
```

---

## Build Project

```bash
npm run build
```

This compiles TypeScript into JavaScript.

Output:

```text
dist/
```

---

## Start Development Server

```bash
npm run dev
```

---

## Environment Variables

Create a `.env` file:

```env
PORT=5000

MONGO_URI=
```

---

## Run Application

### Development Mode

```bash
npm run dev
```

### Production Mode

Build:

```bash
npm run build
```

Start:

```bash
npm start
```

---

## Current Tech Stack

* Node.js 26
* TypeScript
* Express.js
* MongoDB
* Mongoose
* JWT Authentication
* Google OAuth
* Axios
* CORS
* Dotenv
* ts-node-dev
* Concurrently

---

## Verify Installation

```bash
node -v
npm -v
tsc -v
```

Expected:

```bash
Node.js v26.x.x
npm 11.x.x
TypeScript 6.x.x
```

This setup can be reused for all Feasto microservices (`auth`, `user`, `restaurant`, `order`, `payment`, `delivery`) to maintain a consistent architecture.
